#include<bits/stdc++.h>
using namespace std;
int main(){
    long long n, k, i, temp; cin>>n>>k;
    long long ev[n/2], odd[n/2];
    if(k>n/2){
        k=k-n/2;
        cout<<2*k;
    }
    else if(k<n/2){
        cout<<k*3;
    }
}
// https://vjudge.net/contest/361974
